

/**
 * Performs filter operations on Similarity lists. Will be coded as needed.
 */
export default class FilterUtils {

    /* Obviously don't actually make this method. */
    static findSimsWithoutProgramAndOfSizeNewSimSize(sims: import("./PHSimilarity").default[], newSimSize: number): import("./PHSimilarity").default[] {
        throw new Error("Method not implemented.");
    }

}